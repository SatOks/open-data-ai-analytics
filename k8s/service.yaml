# Service for ODAA Web Application
apiVersion: v1
kind: Service
metadata:
  name: odaa-web
  namespace: odaa
  labels:
    app: odaa-web
spec:
  type: NodePort
  selector:
    app: odaa-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30088
